module.exports = {
  end: 'Does not perform any manipulations on the object. This is simply used to simulate a closing tag should you prefer it.',
  getBlocks: 'Builds the object and returns an array with the (built) Blocks.',
  buildToJSON: 'Builds the object and returns a JSON string compatible with the Slack API.',
  buildToObject: 'Builds the object and returns an object compatible with the Slack API (after stringifying it).',
  printPreviewUrl: 'Builds the object and prints a preview URL to Slack\'s Block Kit Builder website to the console.',
};